Sem dávej vlastní bash skripty pro Raspberry Pi.
Deploy je zkopíruje do /opt/agentAdmin/scripts/ (spolu s balíčkem hanz-agent-tools-v1).

Spuštění deploy z PC (PowerShell, v kořeni projektu):

  .venv\Scripts\python scripts\deploy_pi.py
